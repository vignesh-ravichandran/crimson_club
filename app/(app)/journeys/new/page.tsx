/**
 * Create journey: multi-step wizard (basics → dimensions → labels → optional invite).
 */
import { CreateJourneyWizard } from "@/components/domain/CreateJourneyWizard";

export default function NewJourneyPage() {
  return <CreateJourneyWizard />;
}
