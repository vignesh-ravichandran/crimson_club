# Testing conventions

**Status:** Add project-specific rules here as the test suite grows — runner (e.g. Vitest/Jest), file naming, colocated vs `tests/`, coverage expectations, mocks, E2E tooling.

Until expanded, follow patterns in existing tests and keep behavior aligned with `docs/lld/` contracts.

---

_Last updated: 2026-03-20_
