# Input — task files and assets (not committed)

Drop **files and assets** here for a specific task (screenshots, specs, CSVs, briefs). Refer to them in prompts (e.g. “read `input/specs/`”).

---

## Policy (this repo)

- **Git:** Everything under `input/` is **ignored** except this **`README.md`** — your drops are **not** tracked. See root `.gitignore`.
- **Not** the long-term source of truth — merge outcomes into `docs/`, `plans/`, or code.

---

## How to use

1. Add files under `input/` (subfolders optional).
2. Point the AI or task at paths under `input/`.
3. Remove local files when done; keep processed output in `docs/`, `plans/`, or the codebase.

---

_Last updated: 2026-03-20_
